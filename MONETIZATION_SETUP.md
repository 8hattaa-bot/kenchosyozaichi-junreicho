# 収益化の本番切り替え手順

AdMob と楽天アフィリエイトの本番 ID を取り込むまでの手順。

**役割分担**

| 作業 | 担当 |
|---|---|
| アカウント作成、本人確認、口座情報の登録 | **あなた** |
| 管理画面での操作、ID の発行 | **あなた** |
| 発行された ID をコードに差し込む | **こちらで実施** |
| 差し込み後の検証・ビルド | **こちらで実施** |

口座情報・住所・本人確認書類の入力はこちらでは行いません。
**ID の文字列だけ**を伝えてもらえれば、あとは差し込みます。

---

## A. AdMob（バナー広告）

### A-1. アカウントを作る

<https://admob.google.com/>

- 既存の Google アカウントでログイン
- 国／タイムゾーン／通貨を選ぶ（**通貨は後から変更できない**）
- 利用規約に同意

### A-2. お支払い情報を登録

「お支払い」→「お支払い方法を追加」

- 氏名・住所（**開業届と同じ表記にそろえる**）
- **銀行口座**（受け取り用）

> 収益が支払基準額（通常 $100 相当）に達するまで振込は発生しません。
> 口座登録は後回しでも広告表示自体は始められます。

### A-3. アプリを登録

「アプリ」→「アプリを追加」

| 質問 | 答え |
|---|---|
| プラットフォーム | **Android** |
| アプリストアに登録済みですか | **いいえ**（まだ未公開のため） |
| アプリ名 | `県庁所在地巡礼帳` |

登録すると **アプリ ID** が発行されます。

```
形式: ca-app-pub-0000000000000000~0000000000
                                 ↑ 区切りは半角チルダ「~」
```

**これをメモしてください（1つ目）。**

### A-4. 広告ユニットを作る

作ったアプリを開く →「広告ユニット」→「広告ユニットを追加」

| 項目 | 選ぶもの |
|---|---|
| 広告フォーマット | **バナー** |
| 広告ユニット名 | `メインバナー` など任意 |

発行される **広告ユニット ID** をメモします。

```
形式: ca-app-pub-0000000000000000/0000000000
                                 ↑ 区切りは半角スラッシュ「/」
```

**これをメモしてください（2つ目）。**

> **⚠️ チルダとスラッシュを取り違えないこと。** 前半の16桁は同じなので
> 見分けがつきにくい。区切り文字が「~」ならアプリID、「/」なら広告ユニットID。

### A-5. app-ads.txt（任意だが推奨）

AdMob が `app-ads.txt` の設置を促してくる場合があります。
公開済みの GitHub Pages に置けるので、必要になったら言ってください。

---

## B. 楽天アフィリエイト

### B-1. 登録する

<https://affiliate.rakuten.co.jp/>

- 楽天会員アカウントがあればそのまま使える
- サイト情報の登録が必要（**審査あり**）
- 「サイト」として GitHub Pages の URL を登録できます

```
https://8hattaa-bot.github.io/kenchosyozaichi-junreicho/
```

> アプリ本体の URL を求められた場合、Play ストア公開前は上記の
> 紹介ページで代用できることが多い。弾かれたら相談してください。

### B-2. アフィリエイト ID を取得

管理画面にログイン → 任意の商品でリンクを作成 → 生成された URL を見る

```
https://hb.afl.rakuten.co.jp/hgc/1a2b3c4d.5e6f7a8b.9c0d1e2f.3a4b5c6d/?pc=...
                                 └──────── この部分がアフィリエイトID ────────┘
```

`/hgc/` と次の `/` に挟まれた、**ピリオド区切りの4ブロック**が ID です。

**これをメモしてください（3つ目）。**

---

## C. 3つの ID が揃ったら

次の形で伝えてください。こちらで差し込みます。

```
AdMob アプリID      : ca-app-pub-XXXXXXXXXXXXXXXX~XXXXXXXXXX
AdMob 広告ユニットID : ca-app-pub-XXXXXXXXXXXXXXXX/XXXXXXXXXX
楽天アフィリエイトID : XXXXXXXX.XXXXXXXX.XXXXXXXX.XXXXXXXX
```

### こちらで行う作業

1. `config/monetization.js`
   - `ADMOB_PROD_ANDROID_APP_ID` にアプリID
   - `ADMOB_PROD_BANNER_UNIT_ID` に広告ユニットID
   - `RAKUTEN_AFFILIATE_ID` にアフィリエイトID
   - `USE_TEST_ADS` を `false` に
2. `app.json`
   - `react-native-google-mobile-ads` プラグインの `androidAppId` を本番のアプリIDに

   > **AdMob のアプリIDは2箇所にある。** 片方だけ変えると静かに壊れるため、
   > `npm run check` が両者の一致を検証する。
3. `npm run check` で整合を確認
4. 47都市ぶんのアフィリエイトURLが正しく組み立つか検証
5. ビルド

---

## D. 順番について

**AdMob と楽天は、どちらも審査待ちが発生します。**
D-U-N-S 番号の取得に約1.5ヶ月かかるので、**その待ち時間に A と B を
済ませておく**と、番号が届いた時点で一気に公開まで進めます。

**⚠️ 本番IDに切り替えるのは、ストア公開の直前で構いません。**
未公開のアプリで本番の広告リクエストを出し続けると、AdMob に
「無効なトラフィック」と判定されてアカウントが停止されることがあります。
ID は先に取得しておき、**差し込みは提出直前**にするのが安全です。
